# 活動報名表單 - 測試工程師作業.pdf

【功能說明】
此功能模組提供一個單頁式活動報名表單，旨在讓使用者註冊參與活動。使用者需填寫姓名、Email、選擇票種（免費票或付費票）及指定報名人數（範圍為 1 至 10 人）。若選擇付費票，系統將根據單價 NT$500 乘以報名人數，動態計算並顯示總金額。表單提交後，系統會進行前端驗證，若欄位內容不符合規格則顯示對應錯誤訊息；若所有欄位皆有效，則顯示「報名成功」訊息。

【規格審查與漏洞分析 (B1 Spec Review)】
1.  **[規格模糊]**
    *   **錯誤訊息顯示方式與內容：** 規格僅提及「顯⽰對應錯誤訊息」，但未明確指出錯誤訊息的具體文案、顯示位置（例如：欄位下方、表單頂部）、樣式、以及如何處理多個欄位同時錯誤時的顯示邏輯與優先級。
    *   **成功報名後的行為：** 規格僅說明「成功則顯⽰『報名成功』」，但未定義成功訊息的顯示方式（例如：彈出視窗、頁面內訊息）、持續時間、是否自動關閉、以及表單是否應清空或導向其他頁面。
    *   **Email 驗證格式：** 僅要求「需驗證格式」，但未提供具體的 Email 格式驗證規則（例如：是否支援國際化域名、特定字元限制、長度限制等），可能導致不同實作有差異。
    *   **總金額顯示格式：** 未明確總金額的貨幣符號、是否包含小數點（例如：NT$500 或 NT$500.00）、以及千分位符號等顯示細節。
    *   **UI/UX 互動細節：** 未提及表單欄位的預設值、焦點行為、Tab 鍵導航順序、以及在不同操作狀態下（例如：提交中、驗證失敗後）按鈕或欄位的狀態變化。

2.  **[邊界未定義]**
    *   **姓名欄位：** 未定義姓名欄位的最小/最大長度、允許輸入的字元類型（例如：是否支援中文、英文、數字、特殊符號）、以及是否應自動去除前後空白。
    *   **Email 欄位：** 未定義 Email 欄位的最大長度。
    *   **報名人數欄位：** 雖然定義了 1-10 的範圍，但未明確指出當輸入非數字、小數、負數、或超出範圍的數字時，系統應如何處理（例如：是否自動修正、顯示錯誤訊息）。
    *   **表單提交頻率：** 未提及是否需要防止使用者短時間內重複提交表單（例如：提交按鈕在點擊後禁用）。

3.  **[資安/防呆缺漏]**
    *   **前端驗證的局限性：** 雖然要求「純前端即可」，但作為資深 QA 應指出，純前端驗證無法完全防範惡意提交或繞過驗證，若未來有後端整合，必須在後端再次進行所有驗證。
    *   **輸入內容的潛在風險：** 姓名、Email 等欄位若未進行適當的輸入淨化 (Input Sanitization)，可能存在跨站腳本攻擊 (XSS) 風險，例如使用者在姓名欄位輸入 `<script>alert('XSS')</script>`。
    *   **無障礙設計 (Accessibility)：** 未提及表單是否需要符合無障礙規範，例如為視障使用者提供 ARIA 屬性、鍵盤導航支援等。
    *   **響應式設計：** 未提及表單在不同螢幕尺寸（例如：手機、平板）下的顯示與操作兼容性。
    *   **錯誤訊息的清除機制：** 未定義當使用者修正錯誤後，對應的錯誤訊息是否會自動清除。
    *   **表單重置機制：** 未定義成功提交或取消操作後，表單欄位是否應自動重置。

【測試重點】
1.  **[正向]**
    *   驗證所有必填欄位皆填寫有效資料，選擇「免費票」且報名人數為 1 時，提交表單後是否顯示「報名成功」訊息。
    *   驗證所有必填欄位皆填寫有效資料，選擇「付費票」且報名人數為 1 時，總金額是否正確顯示為 NT$500，提交表單後是否顯示「報名成功」訊息。
    *   驗證所有必填欄位皆填寫有效資料，選擇「付費票」且報名人數為 10 時，總金額是否正確顯示為 NT$5000，提交表單後是否顯示「報名成功」訊息。
    *   驗證在選擇「付費票」後，動態修改報名人數（例如從 1 改為 5），總金額是否即時且正確地更新為 NT$2500。
    *   驗證在已輸入報名人數的情況下，從「免費票」切換至「付費票」時，總金額是否正確顯示並計算。

2.  **[異常]**
    *   驗證姓名欄位為空時，提交表單是否顯示「姓名為必填」的錯誤訊息。
    *   驗證 Email 欄位為空時，提交表單是否顯示「Email 為必填」的錯誤訊息。
    *   驗證 Email 欄位輸入無 `@` 符號的字串時（例如：`testexample.com`），提交表單是否顯示「Email 格式不正確」的錯誤訊息。
    *   驗證 Email 欄位輸入無網域的字串時（例如：`test@.com`），提交表單是否顯示「Email 格式不正確」的錯誤訊息。
    *   驗證報名人數欄位為空時，提交表單是否顯示「報名人數為必填」的錯誤訊息。
    *   驗證報名人數欄位輸入非數字字串時（例如：`abc`），提交表單是否顯示「報名人數格式不正確」的錯誤訊息。
    *   驗證報名人數欄位輸入小數時（例如：`1.5`），提交表單是否顯示「報名人數格式不正確」的錯誤訊息。
    *   驗證同時存在多個欄位驗證失敗時（例如：姓名為空、Email 格式錯誤），提交表單是否同時顯示所有對應的錯誤訊息。
    *   驗證在錯誤訊息顯示後，修正對應欄位為有效值，該欄位的錯誤訊息是否自動消失。

3.  **[邊界]**
    *   驗證姓名欄位輸入單一字元（例如：`A`）時，提交表單是否成功。
    *   驗證姓名欄位輸入包含特殊符號（例如：`張三-李四'`）時，提交表單是否成功。
    *   驗證 Email 欄位輸入最短有效格式（例如：`a@b.co`）時，提交表單是否成功。
    *   驗證報名人數欄位輸入最小值 `1` 時，提交表單是否成功。
    *   驗證報名人數欄位輸入最大值 `10` 時，提交表單是否成功。
    *   驗證報名人數欄位輸入 `0` 時，提交表單是否顯示「報名人數必須介於 1 到 10 之間」的錯誤訊息。
    *   驗證報名人數欄位輸入 `11` 時，提交表單是否顯示「報名人數必須介於 1 到 10 之間」的錯誤訊息。
    *   驗證報名人數欄位輸入負數（例如：`-1`）時，提交表單是否顯示「報名人數格式不正確」或「報名人數必須介於 1 到 10 之間」的錯誤訊息。

4.  **[狀態/權限]**
    *   驗證從「付費票」切換至「免費票」時，總金額顯示欄位是否立即隱藏。
    *   驗證在已選擇「付費票」並輸入報名人數後，切換至「免費票」，再切換回「付費票」時，總金額是否正確重新顯示並計算。
    *   驗證成功提交表單後，所有輸入欄位是否被清空或重置為預設狀態。
    *   驗證提交失敗後，所有輸入欄位是否保留使用者上次輸入的值，且錯誤訊息正確顯示。
    *   驗證在表單提交過程中（若有模擬延遲），提交按鈕是否變為禁用狀態，以防止重複提交。
    *   驗證在輸入部分資料後，不提交，直接刷新頁面，表單是否清空。